export const LEAVE_MESSAGES = {
  overlapDetected: "Overlapping leave detected",
};

/**
 * Backend ids for the Annual leave type in the QA legal entity, used when
 * creating / cancelling leave requests directly via the API.
 */
export const ANNUAL_LEAVE = {
  requestTypeId: 656,
  leaveRegulationId: 737,
};

/** Route fragments used to assert navigation. */
export const LEAVE_ROUTES = {
  login: "/login",
  myLeaves: "/my-leaves",
};
